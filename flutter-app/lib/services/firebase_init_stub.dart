// Firebase初期化 - スタブ版（Web用）

/// Firebaseが初期化済みかどうかを返す
bool get isFirebaseInitialized => false;

Future<bool> initializeFirebase() async {
  print('Firebase initialization skipped (stub)');
  return false;
}
